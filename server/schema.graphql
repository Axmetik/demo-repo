type Query {
    greeting: String,
    jobs: [Job!]
}

type Job {
    id: ID,
    """ This is documentation comment for __title__ field """
    title: String!, #! required field
    description: String,
    date: String!,
    company: Company
}

type Company {
    id: ID,
    name: String!,
    description: String,
}